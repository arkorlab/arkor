{
  "name": "@arkor/ui",
  "version": "0.0.0",
  "description": "Shared design tokens for arkor web surfaces. Source-only; consumers bundle it.",
  "private": true,
  "type": "module",
  "exports": {
    "./theme.css": "./src/theme.css"
  },
  "scripts": {
    "build": "echo 'no build (consumers bundle the source)'"
  },
  "engines": {
    "node": ">=22.22.0"
  }
}
