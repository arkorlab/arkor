---
title: "CLI"
description: "The arkor CLI: dev, build, start, and friends."
---

# CLI

{/* TODO: write the real CLI reference.

Commands to document:
- `arkor init`
- `arkor login` / `arkor logout` / `arkor whoami`
- `arkor dev` (Studio at http://localhost:4000, hot reload)
- `arkor build` (bundle to .arkor/build/index.mjs)
- `arkor start` (run the build artefact)

Note the Next.js-style dev/build/start triad. The older train/jobs/logs commands have been removed.
*/}
